{
  "private": true,
  "workspaces": [
    "core",
    "example-app"
  ],
  "scripts": {
    "start": "concurrently --kill-others 'cd ./core && yarn dev' 'cd example-app && yarn start'"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}