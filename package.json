{
  "private": true,
  "workspaces": [
    "core",
    "example-app"
  ],
  "scripts": {
    "start": "concurrently 'cd ./core && yarn exports:dev' 'cd example-app && yarn dev'"
  },
  "devDependencies": {
    "concurrently": "^7.2.2"
  }
}