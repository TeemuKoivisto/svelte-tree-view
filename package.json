{
  "private": true,
  "workspaces": [
    "core",
    "example-app"
  ],
  "scripts": {
    "start": "concurrently 'cd ./core && yarn exports:dev' 'cd example-app && yarn dev:no-ssr'"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}